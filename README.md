# 📋 ПОЛНАЯ ИНСТРУКЦИЯ ПО РАБОТЕ С ПАРСЕРОМ FSA

## 📦 **ТРЕБОВАНИЯ И УСТАНОВКА**

### 1. Установите Python 3.8+
- Скачайте с [python.org](https://python.org)
- При установке отметьте **"Add Python to PATH"**

### 2. Установите библиотеки
Откройте командную строку/терминал и выполните:

```bash
# 1. Обновите pip
pip install --upgrade pip

# 2. Установите необходимые библиотеки
pip install requests aiohttp beautifulsoup4 openpyxl lxml

# ИЛИ установите все сразу из файла:
pip install -r requirements.txt
```

Файл `requirements.txt`:
```txt
requests>=2.31.0
aiohttp>=3.9.0
beautifulsoup4>=4.12.0
openpyxl>=3.1.0
lxml>=4.9.0
aiofiles>=23.0.0
```

## 🚀 **БЫСТРЫЙ СТАРТ (3 минуты)**

### Вариант A: Если нет своего прокси
```python
# В файле config.py измените:
PROXY_MODE = "none"  # Отключаем прокси
VERIFY_SSL = False   # Отключаем проверку SSL
```

### Вариант B: Если есть прокси
```python
# В файле config.py:
PROXY = "http://ваш_логин:ваш_пароль@ip:порт"
PROXY_MODE = "single"  # Использовать один прокси
VERIFY_SSL = False     # Для прокси обычно нужно отключать
```

### Запуск:
1. **Проверьте прокси** (если используете):
   ```bash
   python check_proxy.py
   ```

2. **Создайте список ссылок**:
   ```bash
   python generate_links.py
   ```

3. **Запустите парсер**:
   ```bash
   python parser.py
   ```

## ⚙️ **ПОДРОБНАЯ НАСТРОЙКА**

### 1. Настройка конфигурации (`config.py`)

#### Основные параметры:
```python
# Диапазон ID для парсинга
ID_RANGE = {
    "start": 38450,   # Начальный ID
    "end": 40000,     # Конечный ID (макс: ~120000)
    "step": 1         # Шаг
}

# Настройки производительности
MAX_CONCURRENT_REQUESTS = 10  # Одновременных запросов
REQUEST_TIMEOUT = 30          # Таймаут в секундах
RETRY_ATTEMPTS = 3            # Попыток при ошибке
```

#### Режимы работы с прокси:
```python
# Режим 1: Без прокси (для тестирования)
PROXY_MODE = "none"
VERIFY_SSL = True

# Режим 2: Один прокси
PROXY_MODE = "single"
PROXY = "http://логин:пароль@ip:порт"

# Режим 3: Ротация нескольких прокси
PROXY_MODE = "rotate"
PROXY_LIST = [
    "http://proxy1:порт",
    "http://proxy2:порт",
    "http://proxy3:порт"
]
```

### 2. Форматы вывода данных
```python
OUTPUT_FORMATS = ["excel", "csv", "json"]  # Выберите нужные

# Или только Excel:
OUTPUT_FORMATS = ["excel"]
```

## 📁 **СТРУКТУРА ПРОЕКТА**

```
fsa_parser/
├── 📄 parser.py           # Основной парсер
├── 📄 config.py          # Конфигурация
├── 📄 check_proxy.py     # Проверка прокси
├── 📄 generate_links.py  # Генератор ссылок
├── 📄 requirements.txt   # Зависимости
│
├── 📂 data/              # Папка для данных
│   ├── 📄 all_links.txt          # Список ссылок
│   ├── 📄 fsa_companies_data.xlsx  # Основной Excel файл
│   ├── 📄 fsa_companies_data.csv   # CSV экспорт
│   ├── 📄 fsa_companies_data.json  # JSON экспорт
│   └── 📄 parsing_stats.json       # Статистика
│
└── 📄 parser.log         # Логи работы
```

## 🔧 **РЕШЕНИЕ ПРОБЛЕМ**

### Проблема 1: "Прокси не работает"
**Решение:**
```bash
# 1. Проверьте подключение без прокси
python test_simple.py

# 2. Проверьте прокси отдельно
python check_proxy.py

# 3. Если прокси не работает, отключите его:
# В config.py: PROXY_MODE = "none"
```

### Проблема 2: "Connection timeout"
**Решение:**
```python
# Увеличьте таймауты в config.py:
REQUEST_TIMEOUT = 60  # 60 секунд
MAX_CONCURRENT_REQUESTS = 3  # Уменьшите параллелизм
```

### Проблема 3: "SSL ошибка"
**Решение:**
```python
# В config.py:
VERIFY_SSL = False  # Отключить проверку SSL
```

### Проблема 4: "Мало данных в результатах"
**Решение:**
- Увеличьте диапазон ID (ID_RANGE)
- Проверьте, открываются ли ссылки в браузере
- Возможно, сайт изменил структуру

## 📊 **ЧТО ПАРСЕР СОБИРАЕТ**

Парсер извлекает 23 поля данных:
1. **Основные реквизиты**: ИНН, ОГРН, КПП
2. **Контакты**: телефоны, email, сайт, адрес
3. **Информация о компании**: название, статус, руководитель
4. **Технические данные**: размер страницы, HTTP статус, время обработки
5. **Метаданные**: дата парсинга, использованный прокси

## ⏱️ **ПРИМЕРНЫЕ СРОКИ ОБРАБОТКИ**

| Количество ссылок | Без прокси | С прокси | Память |
|------------------|------------|----------|--------|
| 100              | 2-3 мин    | 5-10 мин | ~50 MB |
| 1 000            | 20-30 мин  | 1-2 часа | ~100 MB |
| 10 000           | 3-5 часов  | 8-12 часов | ~500 MB |

## 💡 **СОВЕТЫ ПО ОПТИМИЗАЦИИ**

### Для скорости:
```python
# config.py
MAX_CONCURRENT_REQUESTS = 20  # Больше параллельных запросов
REQUEST_TIMEOUT = 10          # Меньше таймаут
```

### Для стабильности:
```python
MAX_CONCURRENT_REQUESTS = 5   # Меньше параллельных запросов
REQUEST_TIMEOUT = 30          # Больше таймаут
RETRY_ATTEMPTS = 5            # Больше попыток
```

### Для экономии памяти:
```python
OUTPUT_FORMATS = ["excel"]  # Только один формат
SAVE_EVERY = 100            # Реже сохраняем на диск
```

## 🛡️ **БЕЗОПАСНОСТЬ И ЛЕГАЛЬНОСТЬ**

### Правила использования:
1. ✅ **Разрешено**: Парсинг публичных данных FSA
2. ✅ **Разрешено**: Использование для анализа рынка
3. ✅ **Разрешено**: Создание отчетов для внутреннего использования

### Ограничения:
1. ⚠️ **Не злоупотребляйте**: Делайте паузы между запросами
2. ⚠️ **Соблюдайте robots.txt**: Проверьте правила сайта
3. ⚠️ **Используйте прокси**: Для больших объемов данных

## 📈 **АНАЛИЗ РЕЗУЛЬТАТОВ**

После завершения парсинга проверьте:

### 1. Основной файл:
```python
import pandas as pd

# Загрузите данные
df = pd.read_excel('fsa_companies_data.xlsx')

# Основная статистика
print(f"Всего записей: {len(df)}")
print(f"Успешных: {df[df['Статус'] == 200].shape[0]}")
print(f"С ИНН: {df[df['ИНН'] != ''].shape[0]}")
print(f"С телефонами: {df[df['Телефоны'] != ''].shape[0]}")
```

### 2. Статистика:
```python
import json

with open('parsing_stats.json', 'r', encoding='utf-8') as f:
    stats = json.load(f)

print(f"Успешность: {stats.get('success_rate', 0)}%")
print(f"Общее время: {stats.get('total_time', 0):.1f} сек")
```

## 🔄 **АВТОМАТИЗАЦИЯ**

### Запуск по расписанию (Windows):
1. Создайте файл `run_parser.bat`:
```bat
@echo off
cd /d C:\путь\к\папке\с\парсером
python parser.py
pause
```

2. Настройте Планировщик задач Windows

### Запуск по расписанию (Linux/macOS):
```bash
# Добавьте в crontab:
0 2 * * * cd /путь/к/парсеру && python3 parser.py >> parser.log 2>&1
```

## 📞 **ТЕХНИЧЕСКАЯ ПОДДЕРЖКА**

### Если ничего не работает:
1. **Сделайте минимальный тест**:
   ```bash
   python test_simple.py
   ```

2. **Проверьте логи**:
   ```bash
   type parser.log  # Windows
   cat parser.log   # Linux/macOS
   ```

3. **Создайте issue с информацией**:
   - Версия Python: `python --version`
   - Ошибка из логов
   - Содержимое `config.py` (без паролей)

### Частые ошибки и решения:

| Ошибка | Решение |
|--------|---------|
| `ModuleNotFoundError` | `pip install имя_модуля` |
| `Connection timeout` | Увеличьте `REQUEST_TIMEOUT` |
| `Proxy error` | Отключите прокси или проверьте настройки |
| `SSL error` | Установите `VERIFY_SSL = False` |
| `Memory error` | Уменьшите диапазон ID или `MAX_CONCURRENT_REQUESTS` |

## 🎯 **ЧЕК-ЛИСТ ПЕРЕД ЗАПУСКОМ**

- [ ] Установлен Python 3.8+
- [ ] Установлены все библиотеки
- [ ] Настроен `config.py`
- [ ] Проверен прокси (если используется)
- [ ] Достаточно свободного места на диске (1 ГБ)
- [ ] Стабильное интернет-соединение
- [ ] Антивирус не блокирует скрипт

## 📝 **ПРИМЕР КОНФИГУРАЦИИ ДЛЯ ПЕРВОГО ЗАПУСКА**

```python
# config.py для тестирования

# Отключаем прокси для первого теста
PROXY_MODE = "none"
VERIFY_SSL = False

# Маленький диапазон для теста
ID_RANGE = {
    "start": 38450,
    "end": 38460,  # Всего 10 записей
    "step": 1
}

# Консервативные настройки
MAX_CONCURRENT_REQUESTS = 3
REQUEST_TIMEOUT = 30

# Только Excel для начала
OUTPUT_FORMATS = ["excel"]
```

**Запуск теста:**
```bash
python generate_links.py
python parser.py
```

После успешного теста увеличьте диапазон ID и настройте прокси.

---

## ✅ **ФИНАЛЬНЫЕ ШАГИ**

1. **Настройте параметры** в `config.py`
2. **Запустите генерацию ссылок**: `python generate_links.py`
3. **Запустите парсер**: `python parser.py`
4. **Проверьте результаты** в папке `data/`
5. **Настройте автоматизацию** при необходимости

**Готово!** Ваши данные будут в файлах:
- `data/fsa_companies_data.xlsx` - основной Excel файл
- `data/parsing_stats.json` - статистика парсинга
- `parser.log` - подробные логи выполнения

Удачи в парсинге! 🚀
