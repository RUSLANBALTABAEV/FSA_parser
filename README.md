# Парсер данных FSA (Федеральная служба аккредитации)

Этот парсер собирает ВСЕ данные со всех компаний из реестра FSA.

## 📋 Требования

- Python 3.8 или выше
- Доступ в интернет
- Браузер для получения токена

## 🚀 Быстрый старт

### Windows:
1. Скачайте все файлы в одну папку
2. Запустите `run.bat`
3. Следуйте инструкциям на экране

### Linux/Mac:
1. Скачайте все файлы в одну папку
2. Дайте права на выполнение: `chmod +x run.sh`
3. Запустите: `./run.sh`
4. Следуйте инструкциям на экране

## 📝 Подробная инструкция

### Шаг 1: Получение токена
1. Запустите `get_token.py`
2. Браузер откроет страницу FSA
3. Нажмите F12 → вкладка "Network"
4. Обновите страницу (F5)
5. Найдите запрос к API (содержит `/api/v1/`)
6. Скопируйте значение заголовка `Authorization`
7. Вставьте токен в программу

### Шаг 2: Сбор ссылок
1. Запустите `collect_links.py`
2. Скрипт соберет ВСЕ ссылки на компании
3. Результат сохранится в `all_links.txt`

### Шаг 3: Парсинг данных
1. Запустите `main.py`
2. Скрипт обработает все компании
3. Результаты будут в Excel и CSV файлах

## 📊 Выходные данные

После работы парсера создаются файлы:

1. **`all_links.txt`** - все ссылки на компании (формат: `https://pub.fsa.gov.ru/ral/view/{ID}/current-aa`)
2. **`FSA_all_companies_YYYYMMDD_HHMMSS.xlsx`** - Excel файл со всеми данными
3. **`FSA_all_companies_YYYYMMDD_HHMMSS.csv`** - CSV файл со всеми данными
4. **`links_stats.json`** - статистика сбора ссылок
5. **`parsing_stats.json`** - статистика парсинга

## ⚡ Производительность

- **Сбор ссылок:** 10-30 минут (в зависимости от скорости сети)
- **Парсинг 100 компаний:** ~2 минуты
- **Парсинг 1000 компаний:** ~15 минут
- **Парсинг 38000 компаний:** 8-12 часов

## ⚠️ Важные замечания

1. Токен действует ограниченное время (обычно несколько часов)
2. Если парсинг прервался, можно запустить заново - скрипт продолжит с места останова
3. Для экономии времени можно протестировать на 10 компаниях
4. Рекомендуется запускать полный парсинг на ночь

## 🔧 Структура проекта

- `get_token.py` - получение токена (ПЕРВЫЙ ШАГ)
- `collect_links.py` - сбор всех ссылок
- `main.py` - основной парсер данных
- `config.py` - конфигурация с токеном
- `run.bat` - запуск на Windows
- `run.sh` - запуск на Linux/Mac

## 📞 Поддержка

При возникновении проблем:
1. Проверьте, что токен действителен
2. Убедитесь, что есть доступ к `https://pub.fsa.gov.ru`
3. Проверьте, что все библиотеки установлены
4. Запустите тестовый режим на 10 компаниях

Удачи в парсинге! 🚀
