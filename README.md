# Парсер реестров FSA (38000+ компаний) - Полная инструкция

## 📋 Описание
Автоматический парсер для сбора всех данных из реестра аккредитованных лиц Федеральной службы по аккредитации (ФСА). Программа собирает данные о 38000+ компаниях и сохраняет их в структурированном Excel файле.

## 🚀 Быстрый старт (Windows)

### 1. **Первоначальная настройка**
```bash
# Запустите быстрый старт для первоначальной настройки
quick_start.bat
```

### 2. **Альтернативно вручную:**
1. Скачайте все файлы проекта в одну папку
2. Установите Python 3.8+
3. Установите зависимости:
   ```bash
   pip install aiohttp openpyxl
   ```

## 🔑 Получение токена (ОБЯЗАТЕЛЬНЫЙ ШАГ)

### Метод 1: Автоматический (рекомендуется)
```bash
python get_token.py
```
Скрипт откроет браузер и поможет получить свежий токен.

### Метод 2: Вручную
1. Откройте https://pub.fsa.gov.ru/ral
2. Нажмите **F12** → вкладка **Network** (Сеть)
3. Обновите страницу (**F5**)
4. Найдите любой запрос к API (содержит `/api/v1/`)
5. Скопируйте заголовок **Authorization**
6. Вставьте в `config.py`:
   ```python
   TOKEN = "Bearer eyJhbGciOiJFZERTQSJ9..."
   BASE_URL = "https://pub.fsa.gov.ru"
   API_BASE = "https://pub.fsa.gov.ru/api/v1"
   ```

## 📁 Структура проекта

```
├── config.py              # Настройки и токен
├── collect_links.py       # Сбор всех ссылок на компании
├── main.py               # Основной парсер данных
├── get_token.py          # Получение токена
├── increase_links.py     # Дополнительный сбор ссылок
├── run_all.bat           # Автоматический запуск (Windows)
├── run_parser.sh         # Автоматический запуск (Linux/Mac)
├── all_links.txt         # Собранные ссылки (результат)
├── requirements.txt      # Зависимости Python
└── README.md            # Эта инструкция
```

## 🎯 Возможности

### ✅ Основные функции:
- Сбор данных о 38000+ компаниях из реестра ФСА
- Автоматическое получение свежего токена авторизации
- Парсинг полной информации о каждой компании
- Сохранение в Excel с русскими названиями столбцов
- Поддержка многопоточной обработки (до 25 одновременных запросов)

### ✅ Собираемые данные:
- Основная информация компании (ИНН, ОГРН, КПП, ОКПО и т.д.)
- Контактные данные (телефон, email, сайт, адрес)
- Информация о руководителе
- Данные об аккредитации (номер, даты, статус)
- Декларации соответствия
- Область аккредитации

### ✅ Особенности:
- Русские заголовки столбцов в Excel
- Автоматическое форматирование ширины столбцов
- Промежуточное сохранение для защиты от сбоев
- Подробное логирование процесса
- Статистика выполнения

## ⏱️ Время выполнения

| Количество компаний | Приблизительное время | Рекомендации |
|-------------------|----------------------|-------------|
| 10 компаний       | 30-60 секунд         | Для тестирования |
| 100 компаний      | 3-5 минут            | Быстрая проверка |
| 1000 компаний     | 30-60 минут          | Обычная задача |
| 38000+ компаний   | 4-8 часов            | **Запускать на ночь** |

## 🚀 Запуск парсинга

### **Вариант 1: Полный автоматический запуск (Windows)**
```bash
run_all.bat
```
Скрипт предложит:
1. Проверить Python и библиотеки
2. Выбрать режим (тестовый на 50 компаний или полный)
3. Запустить сбор ссылок и парсинг

### **Вариант 2: Поэтапный запуск**

#### **Шаг 1: Сбор всех ссылок на компании**
```bash
python collect_links.py
```
**Результат:** `all_links.txt` с 38000+ ссылок

#### **Шаг 2: Основной парсинг данных**
```bash
python main.py
```
**Результат:** `FSA_реестры_все_ГГГГММДД_ЧЧММСС.xlsx`

### **Вариант 3: Только тестовый режим**
```bash
# Создать тестовый файл с 50 компаниями
python -c "with open('all_links.txt', 'w') as f: [f.write(f'https://pub.fsa.gov.ru/ral/view/{i}/current-aa\\n') for i in range(38453, 38503)]"

# Запустить парсинг только 50 компаний
python main.py
```

## 📊 Выходные файлы

После успешного выполнения создаются:

### **Основные файлы:**
1. `FSA_реестры_все_ГГГГММДД_ЧЧММСС.xlsx` - полные данные в Excel
2. `FSA_реестры_все_ГГГГММДД_ЧЧММСС.csv` - те же данные в CSV
3. `all_links.txt` - все собранные ссылки на компании

### **Вспомогательные файлы:**
4. `parsing_stats.json` - статистика парсинга
5. `links_stats.json` - статистика сбора ссылок
6. `test_1000_links.txt` - первые 1000 ссылок для тестирования
7. `all_links_part_*.txt` - ссылки, разбитые на части

### **Для отладки:**
8. `first_page.json` - пример ответа API
9. `collection_stats.json` - статистика сбора

## 📈 Столбцы в Excel файле

### **Основная информация:**
- ID компании, Полное наименование, Сокращенное наименование
- ИНН, КПП, ОГРН, Дата ОГРН
- ОКПО, ОКАТО, ОКТМО, ОКФС, ОКОПФ, ОКОГУ, ОКВЭД

### **Контактные данные:**
- Телефон, Факс, Email, Веб-сайт
- Полный адрес (с разбивкой на индекс, регион, город, улицу, дом)

### **Руководство:**
- ФИО руководителя, Должность

### **Аккредитация:**
- Номер аккредитации, Даты (начала и окончания)
- Статус аккредитации, ID файла области аккредитации

### **Декларации:**
- Номер и дата декларации, Статус
- ФИО декларанта, Должность декларанта
- Область аккредитации (код и наименование)

## ⚠️ Важные моменты

### **1. Токен авторизации:**
- Токен действует ограниченное время (обычно несколько часов)
- При ошибке 401 получите новый токен
- Для длительного парсинга может потребоваться обновить токен

### **2. Ограничения и требования:**
- **Интернет:** стабильное соединение без перебоев
- **Память:** ~2 ГБ свободного места на диске
- **Память ОЗУ:** минимум 2 ГБ свободной оперативной памяти
- **Время:** для полного парсинга лучше запускать на ночь

### **3. Возможные проблемы и решения:**

| Проблема | Решение |
|----------|---------|
| Ошибка 401 (Unauthorized) | Получите новый токен через `get_token.py` |
| Собралось мало ссылок | Запустите `increase_links.py` |
| Долго грузится одна страница | Увеличьте таймаут в `config.py` |
| Прервалось на середине | Запустите снова, скрипт продолжит с места остановки |
| Нет заголовков в Excel | Обновите `main.py` из этой инструкции |

### **4. Оптимизация скорости:**
- Увеличьте `CONCURRENCY` в `main.py` (но не более 50)
- Используйте `BATCH_SIZE = 1000` в `collect_links.py`
- Закройте другие программы, потребляющие интернет

## 🔧 Расширенные возможности

### **Увеличение количества ссылок:**
Если в `all_links.txt` мало ссылок:
```bash
python increase_links.py
```
Выберите метод сканирования для поиска дополнительных компаний.

### **Ручная настройка параметров:**
В `config.py` можно изменить:
```python
# Увеличение производительности
CONCURRENCY = 50  # Больше одновременных запросов
BATCH_SIZE = 2000  # Больше компаний в одном запросе
REQUEST_TIMEOUT = 120  # Увеличение таймаута
```

### **Мониторинг прогресса:**
Во время работы в консоли отображается:
- Текущий прогресс в процентах
- Количество обработанных компаний
- Оставшееся время
- Скорость обработки

## 🆘 Поиск и устранение неисправностей

### **1. "Токен недействителен"**
```bash
# Получите новый токен
python get_token.py

# Или вручную обновите config.py
```

### **2. "Собрано только 3 ссылки"**
```bash
# Используйте альтернативный сборщик
python increase_links.py
```

### **3. "Долго висит на одной странице"**
В `collect_links.py` увеличьте:
```python
timeout = aiohttp.ClientTimeout(total=600)  # Увеличить до 600 секунд
```

### **4. "Не хватает памяти"**
Уменьшите `CONCURRENCY` в `main.py`:
```python
CONCURRENCY = 10  # Вместо 25
```

### **5. "Нет заголовков в Excel"**
Обновите `main.py` из этой инструкции - проблема в функции сохранения.

## 📞 Поддержка

### **Перед обращением проверьте:**
1. Токен в `config.py` свежий и начинается с "Bearer "
2. Python версии 3.8 или выше установлен
3. Установлены все библиотеки: `pip install aiohttp openpyxl`
4. Есть стабильное интернет-соединение
5. Достаточно свободного места на диске

### **Логи для отладки:**
Все ошибки и прогресс сохраняются в консоли. Для сложных случаев:
1. Сохраните вывод консоли в файл
2. Проверьте файлы `parsing_stats.json` и `links_stats.json`
3. Посмотрите пример ответа API в `first_page.json`

## 🎯 Краткая инструкция для опытных

```bash
# 1. Получить токен
python get_token.py

# 2. Собрать ссылки (38000+)
python collect_links.py

# 3. Запустить парсинг (4-8 часов)
python main.py

# 4. Результат в FSA_реестры_все_*.xlsx
```

## 📝 Примечания

- Программа предназначена для легального сбора публично доступных данных
- Рекомендуется использовать в ознакомительных целях
- Автор не несет ответственности за использование программы
- При частых запросах сайт может временно блокировать IP
